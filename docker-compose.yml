version: '3.8'

services:
  web:
    build: .
    ports:
      - "5020:5020"
    volumes:
      - ./data/assessments:/app/data/assessments
      - ./static/uploads:/app/static/uploads
    restart: unless-stopped
